(jbuild_version 1)

(executable
 ((name obf)
  (libraries (ppx_deriving.std
              angstrom))
   (preprocess (pps (ppx_deriving.std)))))

(install
 ((section bin)
  (files ((obf.exe as obf)))))
